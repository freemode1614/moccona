
const getAddMessage = () => {
    return `chore(changeset): auto commit`
}

const getVersionMessage = () => {
    return `chore(changeset): 🌟 release`
}


module.exports = {
    getAddMessage,
    getVersionMessage
}