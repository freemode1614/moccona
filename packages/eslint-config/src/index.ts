import config from './main';

const {
    env,
    ignorePatterns,
    overrides,
    parserOptions,
    plugins,
    rules,
} = config;

export {
    env, ignorePatterns, overrides, parserOptions, plugins, rules
}
